[build]
dockerfilePath = "Dockerfile.worker"

[deploy]
startCommand = "node --require tsx/cjs --tsconfig tsconfig.worker.json workers/adsCron.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
